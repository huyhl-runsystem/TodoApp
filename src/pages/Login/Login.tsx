import React from 'react';
import LoginForm from './LoginForm';

const Login: React.FC = () => {
  return (
    <div>
      <h2>Đăng nhập</h2>
      <LoginForm />
    </div>
  );
};

export default Login;